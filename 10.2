t = [1, 2, 3]
new_t = []
def cumsum(t):
    initial = 0
    for num in t:
        cummulative = initial + num
        initial = initial + num
        new_t.append(cummulative)
    return cummulative

cumsum(t)
print(new_t)